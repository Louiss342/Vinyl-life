# 依赖
node_modules/

# 构建产物（发布走 Release 附件，不入库）
main.js
main.js.map
server.js
server.js.map
src/core/gateway-bundle.ts
src/core/style-bundle.ts

# 凭据与运行时状态（绝不入库）
.cookie
.anon-token
.qq-cookie
.qq-guid
.device-id
# 插件设置与播放统计（含用户自己的目录配置与专辑播放记录，属个人数据）
data.json

# 网关日志与临时文件
gateway.log
.gateway.pid
qr-login-tmp*.png
qr-802-debug.json
tmp/
*.log
# lint / 检查产物（含本地绝对路径，不入库）
.eslint-current.json

# 系统与编辑器
.DS_Store
.obsidian/
