# Vinyl Life 数据备份与跨设备迁移

## 备份与恢复

在「设置 → Vinyl Life → 统计」点击「备份设置与统计」，插件会在专辑笔记目录的上一级 `Backups/` 创建带时间戳的 JSON 文件（默认是 `Vinyl Life/Backups/`）。备份包含插件设置、播放统计、已删除专辑的历史封面缓存和上次播放位置；不包含在线平台登录凭据，也不复制库外音频。

点击「恢复备份」并选择该 JSON 文件，可恢复设置与统计。恢复前插件会自动把当前状态另存一份备份。恢复后请重启 Obsidian，再继续使用 Vinyl Life；重启前插件会暂停保存旧会话状态，以免覆盖刚恢复的数据。

统计的累计播放次数长期保留。逐次播放事件只保留最近两年、最多 50,000 条；超过范围的明细不会出现在日历或逐日导出中，累计次数仍在。

**裁剪前会自动归档**：插件每次启动都会检查明细是否已超期、连续使用中每到上限也检查一次；一旦真的要丢掉数据，就先把**完整明细**写成一份备份（同一格式，落在 `Backups/`，文件名以 `Vinyl Life events archive` 开头），再执行裁剪，并弹一条提示告诉你归档在哪。归档失败就不裁（完整明细留在原处，并提示你原因）。到达上限时按**低水位**一批裁掉（裁到 45,000），不是每超一条裁一次 —— 否则连续播放会变成每条播放都归档一次。归档文件用「恢复备份」就能载回 —— 很久没打开插件、来不及手动备份的情况也不会丢逐日记录。

**每周自动备份**（可在「历史 → 数据管理」关掉）：一周没备份过就自动写一份，文件名以 `Vinyl Life auto backup` 开头，只保留最近 N 份（默认 3），清理时**只动自动备份** —— 手动备份与裁剪归档一份都不删。失败会提示且不更新「最近成功备份」，历史页那一行始终显示真正最近一次成功的时间。若需要长期留存，仍建议定期手动备份。

## 换到另一台电脑

1. 同步或复制整个笔记库，包括专辑笔记、库内封面和库内音频。
2. 在旧设备的统计页创建备份，确保备份所在的 `Backups/` 目录随笔记库同步。
3. 在新设备安装同版本或更新版本的 Vinyl Life，在统计页恢复备份，然后重启 Obsidian。
4. 重新登录在线音乐平台。库外音频使用绝对路径；在新设备移动后，用「收藏健康检查」找出失效引用，并在对应专辑笔记中修正路径或重新导入。

若同步工具也同步 `.obsidian/plugins/vinyl-life/data.json`，它本身包含设置和统计；手动备份仍可作为回退点。不要把插件目录中的登录凭据文件公开分享。

顺带一句给用网盘的人：同步工具（iCloud / Dropbox / OneDrive / 坚果云）默认会把 `.obsidian` 一起传上去，而登录凭据就在 `.obsidian/plugins/vinyl-life/` 下（`.cookie`、`.qq-cookie`、`.kugou-cookie` 等）。`.gitignore` 挡得住 git，挡不住同步工具 —— 想让凭据只留本机，就把 `.obsidian` 加进同步排除名单（详见 README 第 8.3 节）。

## 收藏与队列

专辑笔记用 `edition` 属性记录发行版本；不同音源的 ID 可共存在同一篇笔记中。在线搜索结果的「关联已有」会让用户选择目标笔记，只添加对应平台 ID，不覆盖已有文字或封面。

播放器的保存按钮在「设置 → 通用 → 路径 → 队列笔记目录」里创建 Markdown 队列笔记（默认 `Vinyl Life/Queues/`，设置里可改）。**笔记里的曲目列表就是队列本身**：每行是一首（`- [[专辑笔记]] · 曲名`），增删改这些行就等于改队列 —— 改完在命令面板执行「从当前笔记载入队列」，载入按列表来。

曲目按「专辑笔记 + 曲名」还原：同名版本以精确匹配为先，专辑那侧多一截后缀（如 `(Remastered)`）且只有一个候选时才认；对不上的整行跳过并在提示里点名（已删除的专辑、取不到的曲目、改过名的曲目都会落在这里）。
